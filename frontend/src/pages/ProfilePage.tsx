import { Card, CardContent } from '@/components/ui/card';

export function ProfilePage() {
  return (
    <Card>
      <CardContent className="p-6 text-muted-foreground">
        Profile management coming in Phase 6.
      </CardContent>
    </Card>
  );
}
