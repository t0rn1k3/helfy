import { Card, CardContent } from '@/components/ui/card';

export function AddressesPage() {
  return (
    <Card>
      <CardContent className="p-6 text-muted-foreground">
        Address CRUD coming in Phase 6.
      </CardContent>
    </Card>
  );
}
